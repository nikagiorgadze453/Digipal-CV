# Copy to .env.local for local development (never commit .env.local).
# For Vercel/Netlify/etc., set the same variable in the project Environment settings.

# Required: Anthropic API key (server only — not shown in the browser)
ANTHROPIC_API_KEY=

# Alternative name (either works)
# CV_ANTHROPIC_KEY=
